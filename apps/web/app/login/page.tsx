'use client';

export default function LoginPage() {
  return (
    <div className="min-h-screen bg-[var(--bg-primary)] text-[var(--text-primary)] flex items-center justify-center p-6">
      <div className="text-center space-y-4">
        <h1 className="text-3xl font-serif font-bold">Login</h1>
        <p className="text-[var(--text-secondary)]">Welcome back to Elite.</p>
        <p className="text-sm text-[var(--text-tertiary)]">Login flow coming soon</p>
      </div>
    </div>
  );
}
