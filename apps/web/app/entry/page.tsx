import MobileEntry from '@/components/MobileEntry';

export const metadata = {
  title: 'Elite - Start Your Journey',
  description: 'Connect with intention on Elite - premium matchmaking',
};

export default function EntryPage() {
  return <MobileEntry />;
}
