{
  "name": "@elite/shared",
  "version": "0.0.1",
  
  // IF YOUR CODE IS IN A SRC FOLDER:
  "main": "src/index.ts",
  "types": "src/index.ts",
  
  // IF YOUR CODE IS JUST IN THE ROOT OF THE PACKAGE:
  // "main": "index.ts",
  // "types": "index.ts",

  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "echo 'no lint'",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "next": "14.2.35",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^4.0.18"
  }
}
