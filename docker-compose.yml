services:
  db:
    image: postgres:16
    container_name: vael_db
    environment:
      POSTGRES_USER: vael
      POSTGRES_PASSWORD: vael_password
      POSTGRES_DB: vael_mvp
    ports:
      - "5432:5432"
    volumes:
      - vael_db_data:/var/lib/postgresql/data

volumes:
  vael_db_data:
