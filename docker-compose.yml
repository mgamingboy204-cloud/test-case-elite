services:
  db:
    image: postgres:16
    container_name: elite_db
    environment:
      POSTGRES_USER: elite
      POSTGRES_PASSWORD: elite_password
      POSTGRES_DB: elite_mvp
    ports:
      - "5432:5432"
    volumes:
      - elite_db_data:/var/lib/postgresql/data

volumes:
  elite_db_data:
